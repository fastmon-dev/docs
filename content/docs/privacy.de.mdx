---
title: "Privacy: was wir nicht sammeln"
description: Fastmons Datenschutz-Position, Punkt für Punkt. Nützlich, wenn Legal nachfragt.
---

Mit Fastmon sollst du Real-User-Performance messen können, ohne
dafür ein Datenschutz-Minenfeld aufzumachen. Kurz: **keine IPs auf
der Platte, kein roher User-Agent, kein Fingerprinting, kein
Cross-Site-Tracking, EU-Infrastruktur** und ein speicherfreier
Default, der ohne Cookie-Banner auskommt. Auf dieser Seite steht
die ausführliche Version: was die Zusagen in der Praxis heißen,
was wir trotzdem sammeln, damit das Produkt funktioniert, und wie
sich das je nach Collection Mode unterscheidet.

## Was wir nie sammeln, in keinem Modus

Diese Regeln gelten unabhängig davon, in welchem
[Collection Mode](/de/collection-modes) die Site läuft:

| Wir sammeln **kein**…              | Was das praktisch heißt                                                                       |
| ---------------------------------- | --------------------------------------------------------------------------------------------- |
| **IP-Adressen**                    | Die IP wird am Edge entfernt, bevor sie irgendein Application-Code zu Gesicht bekommt. Das Backend liest nur einen vorbereiteten Ländercode (`FM-Country`, zweistelliges ISO), nie die IP selbst. Keine IP wird je auf Disk geschrieben, in keinem Modus. |
| **Roher User-Agent-String**        | Der `User-Agent`-Header wird ebenfalls am Edge entfernt. Das Backend liest nur vorgefertigte `FM-Browser` / `FM-Browser-Version` / `FM-OS` / `FM-Device`-Header. Der rohe UA ist für die Anwendung und ihre Logs nie sichtbar. |
| **Device- oder Browser-Fingerprints** | Kein Canvas-Fingerprinting, keine Font-Enumeration, keine `userAgentData`-High-Entropy-Hints, kein Battery-/Sensor-/AudioContext-Probing. |
| **Cross-Site-IDs**                 | Wir vergeben keine Kennung, die eine Domain-Grenze überlebt. Eine Session auf `acme.com` ist nicht mit einer auf `other.com` verknüpfbar. |
| **Third-Party-Daten**              | Wir reichern nicht mit Datenbrokern, Ad-Networks oder Social-Platforms an. Es gibt keine "Audience"-Daten. |
| **Seiteninhalt**                   | Wir lesen kein DOM, keine Formularwerte, keine Scroll-Positionen, kein Session-Replay. Es gibt keine Aufzeichnung. |
| **URL-Query-Strings oder Hash-Fragmente** | Im Tracker entfernt, bevor irgendein Beacon rausgeht. `/orders/checkout?email=jane@x.com` kommt beim Collector als `/orders/checkout` an. |
| **Personenbezogene Daten**         | Nichts, was der Besucher tippt, klickt oder im Browser hat, wird erfasst.                     |

### Zu Cookies und Storage

`anonymous` ist **strikt speicherfrei**: keine Cookies, kein
`localStorage`, kein `sessionStorage`, kein IndexedDB, keine Cache
API. Deshalb braucht der Modus unter TDDDG §25 / ePrivacy
Art. 5(3) keinen Cookie-Banner.

`consent` ist **vor Opt-In** ebenfalls speicherfrei: auf dem Wire
byte-identisch zu `anonymous`. Bei Opt-In setzt der Tracker ein
`_fm_consent`-Cookie, das die Consent-Entscheidung persistiert, und
schreibt eine Session-ID in den `sessionStorage` (`_s`). Das Cookie
qualifiziert sich nach TDDDG §25(2) Nr. 2 als unbedingt erforderlich:
ohne Persistierung würde das Opt-In den nächsten Seitenwechsel
nicht überleben. Opt-Out löscht beides sofort.

`full` behält die Session-ID im `sessionStorage` ab dem ersten
Pageview. Keine Cookies. Speicher auf dem Gerät ist genau ein
`sessionStorage`-Eintrag, der beim Tab-Schließen verschwindet.

Mehr fasst der Beacon auf dem Gerät des Besuchers nie an. Kein
`localStorage` in irgendeinem Modus. Kein IndexedDB. Keine Cache
API.

## Was wir schon sammeln

Genau das, was wir brauchen, um Web Vitals und Performance pro
Seite auszurechnen:

- **Seiten-URL**: bereinigt je nach
  [Collection Mode](/de/collection-modes).
- **Web Vitals**: LCP, INP, CLS, FCP, TTFB und verwandte Timings.
- **Navigation-Timing**: Nav-Typ, TTFB, Pageload-Timing.
- **Resource-Timing**: pro Kategorie aggregierte Byte-Größen und
  Counts (Script, CSS, Image, Font, Sonstige).
- **Fehler**: Typ, Count und (mit Consent) Message und Frames.
  Alle Messages laufen vor dem Versand durch einen immer aktiven
  Scrubber, der URLs, E-Mail-Adressen, numerische IDs und Strings
  in Anführungszeichen durch `*` maskiert, auch bei explizitem
  Consent. Ein `Bearer eyJhbG…`-Token, der sich in eine Exception
  schleicht, erreicht uns nicht.
- **Umgebung**: Browser-Familie, OS-Familie, Device-Klasse
  (`mobile`/`tablet`/`desktop`), Viewport-Klasse, effektiver
  Connection-Typ, Netzwerk-RTT- und Downlink-Schätzungen. Die
  Netzwerkwerte sind Echtzeit-Messungen, die der Browser beim
  Lesen aus dem laufenden Verkehr berechnet, kein gespeicherter
  Geräte- oder OS-Zustand. `navigator.deviceMemory`,
  `navigator.hardwareConcurrency`, das `saveData`-Flag,
  `prefers-color-scheme` und `prefers-reduced-motion` lesen wir
  nicht mehr: nach der breiten EDPB-Leitlinie 2/2023 fallen die
  unter „Zugriff auf gespeicherte Informationen" gem. TDDDG §25(1),
  und wir haben sie ausgemustert, statt sie hinter ein Consent-Gate zu stellen.
- **Geo**: nur Land (zweistelliges ISO). Wird am Edge aus der IP
  abgeleitet und als Request-Header weitergereicht; die IP selbst
  erreicht die Anwendung nie.
- **Kampagnen-Attribution**: `utm_source` / `utm_medium` /
  `utm_campaign` / `utm_term` / `utm_content` und ein
  Ad-Plattform-Label `click_id_param` (z. B. `"gclid"`), aus der
  Seiten-URL extrahiert. Jedes Feld auf 64 Zeichen begrenzt. Der
  Click-ID-*Wert* (`Cj0KCQabc...`) wird bewusst verworfen; nur
  das Label, welche Plattform den Klick geschickt hat, wird
  gespeichert. Das sind vom Marketer auf den eigenen Outbound-URLs
  gesetzte Tags, keine Besucher-abgeleiteten Daten, und werden in
  jedem Modus gespeichert.
- **Session-ID**: nur in `full`, und in `consent` nachdem der
  Besucher über den CMP der Site Opt-In gegeben hat. Liegt im
  `sessionStorage`, gruppiert Pageviews einer Session, stirbt mit
  dem Tab.
- **Referrer**: vom Tracker auf Origin + Pathname vorbereinigt
  (Query und Hash werden bereits im Browser verworfen). Das
  Backend klassifiziert ihn per
  [Snowplow Referer-Parser](https://github.com/snowplow-referer-parser/referer-parser)
  in eine gebucketete Quelle (`Google` / `Twitter` / `Direct` / …)
  und ein kanonisches Medium (`search` / `social` / `email` /
  `paid` / `chatbot` / `internal` / `unknown`) und verwirft den
  rohen Wert sofort danach, vor jedem Logging-Pfad. **Gespeichert
  werden nur die beiden gebucketeten Spalten** (in jedem Modus);
  die rohe URL landet nie auf der Platte.
- **LCP-Image-URL, LoAF-Script-Pfade, Third-Party-Domain-Map**:
  nur in `full` oder in `consent` nach Opt-In. URLs werden bei der
  Erfassung auf Origin + Pathname reduziert.

Mehr schickt der Beacon nicht. Es gibt auch keinen Opt-In, mit dem
du irgendwo „noch ein bisschen mehr" freischaltest. Das Mehr haben
wir gar nicht gebaut.

### Der am Edge abgeleitete `stitch`-Identifier

Damit per-Besucher-Performance-Debugging über Pageviews hinweg
möglich ist, ohne dass irgendein Besucher-Identifier auf dem Gerät
liegt, berechnet der Edge einen Einweg-Join-Key, bevor IP und
User-Agent zerstört werden:

```
stitch = HMAC-SHA256(rotierendes_Salt, IP | User-Agent | collector_hash)
```

abgeschnitten auf 128 Bit (32 Hex-Zeichen auf dem Wire).
Eigenschaften:

- **Forward Secrecy by Construction.** Das Salt ist ein 32-Byte-
  Wert aus `crypto/rand`, lebt ausschließlich im RAM des aktiven
  Edge und rotiert alle 24 Stunden. Es wird nie persistiert, nie
  verteilt, nie geloggt. Wer später Zugriff auf die Zeile bekommt,
  kann `stitch` nicht zurück auf (IP, UA) rechnen: das Salt, mit
  dem es produziert wurde, ist weg.
- **Verknüpfbarkeit gedeckelt, höchstens ~24 h.** Bei jeder
  Salt-Rotation bekommt derselbe Besucher einen neuen
  `stitch`-Wert. Cross-Pageview-Verknüpfung kann nicht über ein
  Salt-Fenster hinaus anwachsen.
- **Pro Site, nie Cross-Site.** `collector_hash` steckt im
  HMAC-Input, also liefert dieselbe (IP, UA) auf zwei
  unterschiedlichen Kunden-Sites unterschiedliche
  `stitch`-Werte. Die „keine Cross-Site-Identifier"-Zeile in der
  Tabelle oben gilt unverändert.

`stitch` wird in jedem Modus (auch `anonymous`) gespeichert, auf
Grundlage von DSGVO Art. 6 Abs. 1 lit. f. IP und User-Agent
werden am Edge unter derselben Rechtsgrundlage verarbeitet, die
auch den Ländercode produziert; das Backend sieht nur den Output
des HMAC. Die klassische Aussage „im `anonymous`-Modus gibt es
keinen seitenübergreifenden Identifier" wird damit ersetzt durch
„zeitlich begrenzter, forward-secret seitenübergreifender
Identifier". Die explizite, consent-gedeckte `sid` (mehrtägige
Session-ID in `full` / `consent` nach Opt-In) bleibt unverändert.

### Site-Metadaten vs. Besucherdaten

Eine Handvoll Felder beschreibt **die Site des Kunden**, nicht den
Besucher: welche Third-Party-Domains die Seite lädt, der Byte-Mix
nach Script / Image / Font, die Origin des LCP-Bilds. Diese Werte
sind über alle Besucher derselben Seite identisch. Sie sagen dir,
welches deiner CDNs langsam ist; sie identifizieren niemanden.

Unter DSGVO Art. 4(1) liegen sie damit auf Feldebene außerhalb der
Personendaten-Grenze. Wir behandeln sie trotzdem sorgfältig
(Script-URLs und LCP-Image-URLs werden bei der Erfassung auf
Origin + Pathname reduziert), aber sie sind nicht das, worauf der
Consent-/Verknüpfbarkeits-Gate zielt.

## Collection Mode und was sich ändert

Der [Collection Mode](/de/collection-modes) der Site entscheidet
nur über **Verknüpfbarkeit**, nicht über Datenkategorien. Die
Privacy-Untergrenze oben (keine IP-Speicherung, kein roher
User-Agent, keine Fingerprints) gilt in jedem Modus. Was sich
zwischen den Modi unterscheidet: ob `session_id`, LCP-Image-URL,
LoAF-Detail und Third-Party-Domain-Map mitgeschickt werden (und
damit Pageviews zu Sessions verknüpfbar) oder entfernt sind, und
ob Fehler-Messages und Stack-Frames gesendet werden.
Per-Mode-Aufschlüsselung und wie `window.__fastmon_consent(true|false)`
deinen CMP an den Beacon koppelt, stehen unter
[Collection Modes](/de/collection-modes).

## Wo die Daten liegen

- **Nur EU.** Unsere komplette Infrastruktur läuft in der EU. Kein
  Anbieter mit US-Jurisdiktion im Datenpfad: kein Cloudflare, kein
  AWS, kein GCP, kein Vercel.
- **Verantwortlich.** Verarbeitet wird von Fastmon, der Firma
  hinter dem Produkt. Im AVV stehen alle Sub-Auftragsverarbeiter,
  den schicken wir auf Anfrage.
- **Retention.** Per Default 90 Tage. Pro Site einstellbar
  (`data_retention_days` in den Site-Einstellungen). Was abläuft, wird
  weggeworfen, nicht archiviert.

## Vergleich

Wenn du von einem US-Analytics-Produkt umsteigst, hier das relevante
Delta:

| Fähigkeit                        | Fastmon | Typischer US-RUM-Anbieter        |
| -------------------------------- | ------- | -------------------------------- |
| Gesetzte Cookies                 | 0 (1 in `consent`, nach Opt-In) | 1–N (Analytics + Ad-Partner) |
| IP gespeichert                   | Nein    | Oft, manchmal trunkiert          |
| Cross-Site-Identity-Graph        | Nein    | Oft, via SDK oder Pixel-Netz     |
| Session-Replay / DOM-Dump        | Nein    | Häufig als Opt-In                |
| Datenpfad ausschließlich EU      | Ja      | Selten                           |
| Schrems-II-Exposition            | Keine   | Ja (US-Transfermechanismen)      |

## Deine Verantwortung

Der Tracker garantiert die Wire-Form. Zwei Dinge bleiben bei dir:

- **Keine PII in URL-Pfaden.** Query-Strings und Hash-Fragmente
  werden entfernt, der Pathname bleibt erhalten.
  `/orders/jane.doe@x.com` würde die E-Mail in unsere Pipeline
  tragen. Nutze opake IDs (UUIDs, numerische DB-IDs) oder bring PII
  in Query-Parameter, die ohnehin gestrippt werden.
- **Keine PII in UTM-Parametern.** UTM-Tags und Click-ID-Labels
  landen wortwörtlich aus den Outbound-URLs der Marketer in der
  Datenbank. Pack da keine Besucher-PII rein. Bau Kampagnen-URLs
  niemals als `?utm_term=jane@example.com&utm_content=user-12345`
  für Per-Empfänger-Tracking. Jedes Feld wird serverseitig auf 64
  Zeichen gedeckelt, der Wert läuft aber nicht durch den Scrubber.
  Ad-Plattform-Click-ID-*Werte* verwirft der Tracker selbst,
  damit greift das Per-Empfänger-Join-Risiko nur bei den
  UTM-Feldern.
- **Kein roher User-Input in `throw`.** Der immer aktive Scrubber
  fängt gemusterte PII (URLs, E-Mails, IDs), aber freier Text
  (Namen, eigene Bezeichner) kann durchrutschen. Typisiere den
  Fehler: `throw new EmailDeliveryError({ code: "..." })` statt
  `throw new Error("Failed to send to " + email)`. Greift nur unter
  Consent=1 / `full`; ohne Consent wird die Message ohnehin
  komplett verworfen.

## Was das für dich heißt

- **Im `anonymous`-Modus brauchst du für unseren Beacon keinen
  Cookie-Banner.** Keine Cookies, keine Cross-Site-IDs, keine
  IP-Speicherung, speicherfrei auf dem Gerät des Besuchers. Andere
  Tools auf der Seite vielleicht trotzdem, aber das liegt nicht an
  uns.
- **Schrems II ist für unsere Pipeline kein Thema.** Daten verlassen
  die EU schlicht nicht.
- **Deinem Datenschutzbeauftragten (DSB) kannst du eine Ein-Seiter-Antwort geben**: Link auf
  diese Seite plus unseren AVV, fertig.
- **Striktes Consent-Regime?** Nutze den
  [Collection Mode](/de/collection-modes) `consent`.

## Nächste Schritte

- [Collection Modes](/de/collection-modes) ansehen.
- Technische Details zum [Beacon](/de/beacon).
- AVV gewünscht? Mail an [support@fastmon.eu](mailto:support@fastmon.eu).
