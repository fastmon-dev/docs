---
title: Run Synthetic Page
full: true
_openapi:
  preload:
    - ./.fastmon-openapi.json
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Trigger an on-demand analysis (the run completes asynchronously at
          CS).


          Always allowed for an existing page — a paused page (`schedule=false`)
          can still

          be run on demand; the page's recurring schedules are preserved on the
          rerun.

          Uses the page's `nocache` setting unless the `nocache` query param
          overrides it

          for this single run.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./.fastmon-openapi.json" operations={[{"path":"/v1/sites/{site_id}/synthetic/pages/{page_id}/run","method":"post"}]} showDescription />
    </>
  );
}