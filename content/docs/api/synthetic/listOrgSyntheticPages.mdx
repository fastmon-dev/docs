---
title: List Org Synthetic Pages
full: true
_openapi:
  preload:
    - ./.fastmon-openapi.json
  method: GET
  webhook: false
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          List monitored pages across all sites of the org (manual first, then
          auto).


          Org-scoped twin of `list_synthetic_pages`: `SyntheticPage` has no

          `organization_id`, so scope is resolved by joining
          `sites.organization_id`;

          each row still carries its own `site_id`. Optional `site_id` narrows
          to one

          site. `summary=true` attaches the cached latest-result health snapshot
          per

          page (concurrency-capped), same as the site-scoped list.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./.fastmon-openapi.json" operations={[{"path":"/v1/organizations/{org_id}/synthetic/pages","method":"get"}]} showDescription />
    </>
  );
}