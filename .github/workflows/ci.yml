name: CI

on:
  pull_request:
  push:
    branches: [main]

# GitHub-hosted runners on purpose: pull requests from forks run untrusted code
# (npm ci, build), which must never reach the self-hosted deploy runner.
# Only deploy.yml uses self-hosted, and it fires on main only.
permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  checks:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 26
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run lint:prose
      - run: npm run format:check
      # `if: always()` so the typecheck still runs (and reports) even when an
      # earlier check fails — we want both signals from a single run rather
      # than fail-fast hiding the type errors behind a lint error.
      - run: npm run types:check
        if: always()

  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 26
          cache: npm
      - run: npm ci
      # `npm run build` runs `generate:api` first, which fetches the public
      # OpenAPI spec from https://api.fastmon.eu/v1/openapi.json. If that
      # endpoint is down, this step fails — by design, since deploying a
      # build with stale API docs would silently ship outdated reference.
      - run: npm run build
